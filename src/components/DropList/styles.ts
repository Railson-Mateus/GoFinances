import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 328,
    height: 56,
    borderRadius: 5,
  },
  dropList: {
    flex: 1,
    backgroundColor: "#FFFFFF",
    borderRadius: 5,
  },
});
